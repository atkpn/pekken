<h1>ユーザー情報編集</h1>

<%= form_with model: @user, url: user_update_path do |f| %>
  <%= f.label :name, "ユーザー名" %>
  <%= f.text_field :name %>

  <%= f.label :email, "メールアドレス" %>
  <%= f.text_field :email %>

  <%= f.submit "保存" %>
<% end %>

<%= link_to "退会する", user_unsubscribe_path %>
